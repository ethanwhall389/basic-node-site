<footer>
    <ul class="footer-links">
        <% links.forEach((link) => { %>
            <li>
                <a href="<%= link.href %>">
                    <span><%= link.text %></span>
                </a>
            </li>
        <% }) %>
    </ul>
</footer>